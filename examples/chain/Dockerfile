FROM golang
ENV HOST_ADDR ''
ENV HOST_PORT 0
ENV LOCAL_ADDR tcp://0.0.0.0
ENV LOCAL_PORT 10001
ENV HTTP_ADDR 0.0.0.0
ENV HTTP_PORT 80
WORKDIR /app
COPY . .
RUN ls -la
RUN go mod tidy
RUN go build -o app .
CMD ["./app"]